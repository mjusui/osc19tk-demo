const { exec }=require('child_process');

const u={
  u: undefined,
};

console.log(u);

console.log(
  JSON.stringify(u)
);

exec('ls /hoge', (err, stdout, stderr)=>{
  const resp={
    err: err,
    stdout: stdout,
    stderr: stderr,
  };

  console.log(resp);

  console.log(
    JSON.stringify(resp)
  );
});
