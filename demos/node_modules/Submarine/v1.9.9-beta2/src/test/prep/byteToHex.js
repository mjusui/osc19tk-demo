const crypto=require('crypto');


const a=[]
for(let i=0; i < 256; ++i){
  a[i] = (i + 0x100).toString(16).substr(1);
  //a[i] = (i + 0x100).toString(16);
  //a[i] = (i).toString(16).substr(1);
}


const rand=crypto.randomBytes(16)
console.log(
  rand,
  rand.toString('hex')
);


/*console.log(
  a[a.length-1]
);*/

console.log(
  a[rand[0]],
  a[rand[1]],
  a[rand[2]],
  a[rand[3]],
);

console.log(
  rand.map( r => r )
);

console.log(
  rand.map( r => a[r] )
);

/*console.log(
  (0x0a)
);*/

