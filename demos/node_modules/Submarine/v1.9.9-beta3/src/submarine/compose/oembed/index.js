'use strict';
const Compose=require('compose/Class');

/*const template=fs.readFileSync(
  `${__dirname}/files/template.html`
).toString();*/

module.exports=class extends Compose {
  template(){
    return `<iframe src='{{% pathname %}}'></iframe>`;
  }
}
