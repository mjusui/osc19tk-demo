const exec=require('child_process').exec;

console.log(
  JSON.stringify({
    a: 'a',
  })
);

console.log(
  JSON.stringify({
    a: 'a',
    b: 'b',
  })
);

const test={
  msg: 'test',
  msg2: 'test2',
};
console.log(JSON.stringify(test));

exec(`
  bash -c 'echo ${JSON.stringify(test)}'
`, console.log);


console.log(JSON.stringify(
  'string'
));
